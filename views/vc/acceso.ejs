<%- include('../partials/head', { title: title, css: css, themeColor: themeColor }) %>

<style>:root { --juego: #ff4655; }</style>

<main class="vc angosto" style="max-width:420px;padding-top:70px">
  <p class="eyebrow">Virtual Champions</p>
  <h2 style="font-size:30px">Acceso organización</h2>
  <p class="sec-lead" style="margin-bottom:26px">
    El panel del torneo va con su propia contraseña: no es la misma de la Expo.
  </p>

  <% if (!configurado) { %>
    <div class="aviso malo">
      <p style="margin:0">
        El panel todavía no está habilitado. Falta <code>PASSWORD_VC</code> en el
        archivo <code>.env</code> del servidor.
      </p>
    </div>
  <% } %>

  <% if (error) { %>
    <div class="aviso malo"><p style="margin:0"><%= error %></p></div>
  <% } %>

  <form method="POST" action="/vc/acceso">
    <div class="campo">
      <label for="email">Correo institucional</label>
      <input type="email" name="email" id="email" required autofocus
             pattern="<%= PATRON_CORREO %>" value="<%= email %>"
             placeholder="tunombre@<%= DOMINIO %>" />
    </div>
    <div class="campo">
      <label for="password">Contraseña del torneo</label>
      <input type="password" name="password" id="password" required />
    </div>
    <button type="submit" class="btn block">Entrar</button>
  </form>

  <p style="margin-top:26px;text-align:center">
    <a href="/virtual-champions" class="foot-acceso">Volver al torneo</a>
  </p>
</main>

<%- include('../partials/footer') %>
